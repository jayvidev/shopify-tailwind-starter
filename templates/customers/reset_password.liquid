<div class='page section-spacing'>
  <div class='mx-auto max-w-sm'>
    <h1 class='mb-6 text-2xl font-semibold'>{{ 'customer.reset_password.title' | t }}</h1>
    <p class='mb-4 text-sm text-fg-muted'>{{ 'customer.reset_password.subtext' | t }}</p>

    {%- form 'reset_customer_password', class: 'flex flex-col gap-4' -%}
      {{ form.errors | default_errors }}

      {%- assign password_label = 'customer.reset_password.password' | t -%}
      {%- assign confirm_label = 'customer.reset_password.password_confirm' | t -%}
      {% render 'form-input',
        id: 'reset-password',
        name: 'customer[password]',
        type: 'password',
        label: password_label,
        required: true
      %}
      {% render 'form-input',
        id: 'reset-password-confirm',
        name: 'customer[password_confirmation]',
        type: 'password',
        label: confirm_label,
        required: true
      %}

      {%- assign submit_label = 'customer.reset_password.submit' | t -%}
      {% render 'button', label: submit_label, type: 'submit', full: true %}
    {%- endform -%}
  </div>
</div>
